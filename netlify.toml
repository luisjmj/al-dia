# Configuración de despliegue en Netlify
[build]
  command = "npm run build"
  publish = "dist"

# La SPA usa React Router: cualquier ruta debe servir index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
